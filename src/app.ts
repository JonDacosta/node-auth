import { Server } from "./presentation/server";


(() => {
   main(); 
})

async function main() {
    // await base de datos

    //to-to inicio de server
    new Server({
        
    })
        .start
}